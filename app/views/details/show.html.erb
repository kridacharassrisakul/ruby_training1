<p id="notice"><%= notice %></p>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <tr>
    <td><%= @sudoku.name %></td>
  </tr>
</table>

<%= link_to 'Edit', edit_detail_path(@sudoku) %> |
<%= link_to 'Back', details_path %>
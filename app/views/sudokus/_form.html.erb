<%= form_for(@sudoku) do |f| %>
  <% if @sudoku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sudoku.errors.count, "error") %> prohibited this sudoku from being saved:</h2>

      <ul>
      <% @sudoku.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
  <tr>
    <th> Name </th>
  </tr>
  <tr>
    <td><%= f.text_field :name %></td>
  </tr>
  <% @sudoku.details.each do |field| %>
  <%= f.fields_for(field) do |r| %>
  <tr>
  <td><%= '================================================================================' %></td>
  </tr>
  <tr>
    <td><%= r.text_field(:column_1) %><%= r.text_field(:column_2) %><%= r.text_field(:column_3) %><%= r.text_field(:column_4) %><%= r.text_field(:column_5) %><%= r.text_field(:column_6) %><%= r.text_field(:column_7) %><%= r.text_field(:column_8) %><%= r.text_field(:column_9) %></td>
  </tr>
  <% end %>
  <% end %>
</table>
<div class="actions">
    <%= f.submit %>
</div>
<% end %>